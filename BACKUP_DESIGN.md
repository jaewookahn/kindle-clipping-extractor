# 자동 롤링 백업 설계안

작성: 2026-09-20 · 세션 `킨들클리핑new` · **설계안 (미구현)**
결정 근거: `~/prj/reading_manager/TODO.md` §1 — 방침 ③ 자동 롤링 백업 확정

---

## 1. 왜 필요한가 — 실제로 잃었다

2026-09-19 14:11 의 동기화 실행이 `kindle_sync.json` 을 덮어썼다.

```
소실 전   Colorsoft 29권 1,951건   783KB
소실 후   1권 157건                 32KB
```

Time Machine 까지 뒤졌으나 복구 불가로 판정됐다. KSDK 회수본으로 책 수(29권)는
덮을 수 있으나 **본문 단위 완전성은 미증명**이다.

결정적인 점은 이것이 **정상 종료한 실행**이었다는 것이다. 크래시도, 디스크 오류도
아니었다. 입력(KFX 본문)이 조용히 비었고, 코드가 그 결과를 성공으로 간주해
정상적으로 덮어썼다. 즉 **"실행이 성공하면 안전하다"는 가정이 깨진다** —
백업 시점을 실패 처리에 묶으면 이 사고를 못 막는다.

## 2. 대상 — 덮어쓰기로 사라질 수 있는 것

| 파일 | 쓰는 곳 | 잃으면 |
|---|---|---|
| 출력 산출물 (`-o`, 기본 `kindle_sync.json`) | `sync_kfx.run_pipeline` | **위 사고 당사자.** 추출 결과 전체 |
| `~/.kindle_notion_sync.json` | `notion_export.save_state` | fingerprint 상태 → 전량 재업로드·중복 |
| `~/.kindle_kfx_sync.json` | `sync_kfx.save_state` | `seen_keys` → 전량 재처리 |
| `~/.kindle_kfx_titles.json` | `title_cache.save_cache` | 제목·ASIN → 권당 재추출 (느릴 뿐) |
| `~/.kindle_summaries.json` | `summarize.save_cache` | 요약 → **재생성 시 API 비용** |
| `~/.kindle_kfx_text/*.json.gz` | `text_cache.store` | 본문 → **기기 없으면 복구 불가** |

`text_cache` 는 항목 단위로만 쓰고 통째로 덮지 않으므로 우선순위가 낮다. 나머지
다섯은 **매 실행 전체 덮어쓰기**다.

## 3. 설계

### 3.1 위치

```
~/.kindle_backups/<논리이름>/<YYYYMMDD-HHMMSS>.<원래확장자>
```

`<논리이름>` 은 경로가 아니라 역할 이름(`notion_state`, `sync_output`, …).
경로로 키를 잡으면 `-o` 를 바꿀 때마다 계보가 끊긴다.

원본 옆(`.bak`)에 두지 않는 이유: 출력 파일은 사용자가 임의 경로를 주고, 그
디렉터리가 동기화 폴더일 수 있다.

### 3.2 시점 — 쓰기 직전, 예외 없이

각 `save_*` 의 **맨 앞**에서 기존 파일을 복사한다. 실행 성공/실패, dry-run 여부,
내용이 커졌는지 작아졌는지 **판단하지 않는다.** §1 의 교훈이 그것이다.

`dry-run` 은 애초에 쓰지 않으므로 자연히 제외된다.

### 3.3 회전 규칙 — 세대 + 최대치 보존

단순 "최근 N개"만 두면 **사고 후 N번 실행하는 것만으로 정상본이 밀려난다.**
09-19 사고도 이 방식이었으면 몇 번의 재시도로 783KB 본이 사라졌다.

```
보존 = 최근 10세대  ∪  {역대 최대 크기 1개}
```

역대 최대본은 `keep-largest` 표식(파일명 접미사 `.keep`)을 붙여 회전에서 제외한다.
크기는 완전성의 대리 지표일 뿐이지만, **클리핑 산출물은 단조 증가에 가깝다는
성질**이 있어 이 도메인에서는 잘 맞는다.

상한: 논리이름당 10세대 + 1. 출력 산출물이 최대 ~1MB 급이므로 총량은 수십 MB.

### 3.4 급감 경고

새로 쓰려는 내용이 직전 백업보다 **50% 이상 작으면** 경고를 남긴다(중단하지는
않는다 — 정당한 축소도 있다: `--book-exact` 로 한 권만 뽑는 경우).

```
[warn] sync_output 이 783,142B → 32,408B 로 줄어듭니다.
       직전 백업: ~/.kindle_backups/sync_output/20260919-140322.json
```

이 한 줄이 09-19 에 있었다면 사고를 즉시 알아챘을 것이다.

### 3.5 공개 API (예정)

```python
# kindle/backup.py
def snapshot(path: Path, name: str, *, keep: int = 10) -> Optional[Path]
    """path 가 있으면 백업하고 그 경로를 돌려준다. 없으면 None."""
```

각 `save_*` 가 한 줄 추가로 쓴다. 실패해도 **원래 저장을 막지 않는다**
(백업 실패로 동기화가 죽으면 더 나쁘다) — 경고만 남긴다.

## 4. 결정이 필요한 것

| # | 항목 | 제안 |
|---|---|---|
| 1 | 세대 수 | 10 |
| 2 | 급감 임계 | 50% |
| 3 | 백업 위치 | `~/.kindle_backups/` (`KINDLE_BACKUP_DIR` 로 재지정 가능) |
| 4 | 끄는 수단 | `--no-backup` 플래그 |

## 5. 하지 않는 것

- **압축하지 않는다.** 수십 MB 수준이고, 급할 때 `cat` 으로 바로 볼 수 있어야 한다.
- **원격 업로드하지 않는다.** 클리핑 본문은 개인 독서 기록이다.
- **`text_cache` 항목별 백업은 넣지 않는다.** 통째 덮어쓰기가 없어 위험이 낮고,
  넣으면 권당 247KB × 세대로 불어난다.
