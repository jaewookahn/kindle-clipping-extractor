# USBNetwork(USB 위 SSH) 조사 — 켜지 않음, 코드 변경 없음

작성: 2026-09-20 · 세션 `킨들클리핑new` · 지시: 리딩총괄2 · 기기 미연결(문서·백업 조사만)

---

## 결론 먼저

**설치 여부도, 켤 방법도 이 저장소 어디에도 기록이 없다.** 그리고 이미 있는
실측 하나가 이 제안의 전제를 흔든다 — **탈옥 이후에도 기기엔 `ssh`도
`dropbear`도 없다** (`;log mrpi` 로 직접 확인, 아래 1번). USBNetwork 는
별도 패키지 설치가 필요한데, 그 설치·구동에 필요한 커뮤니티 ELF 바이너리가
**바로 이 기기(5.19.x)에서 라이브러리 비호환으로 죽는다는 것이 MRPI 로 이미
실증됐다** — 같은 문제가 dropbear 에도 재현될 가능성이 높다.

---

## 1. Véra 에 USBNetwork 가 포함돼 있는가 — **아니오 (실측)**

`KINDLE_ANNOTATION_OUTAGE.md` §16.6, 원본 데이터
`~/kindle_annotation_backup/device-junk-20260919/documents/ksdk_probe/01_tools.txt`
(탈옥 **후**, `;log mrpi` 로 기기에서 직접 `which` 돌린 결과):

```
ssh -> (없음)      scp -> (없음)      sftp -> (없음)
dropbear -> (없음)  dropbearmulti -> (없음)
python -> (없음)   python3 -> (없음)  perl -> (없음)
wget/curl/nc/telnet/ftpget/ftpput/openssl/busybox/sh/ash/bash → 있음
```

Véra 는 **탈옥(uid=0 획득)만** 한다 — 부가 패키지(USBNetwork, KUAL, MRPI 등)를
같이 심지 않는다. 지금 설치돼 있지 않고, 켜려면 **별도 설치**가 필요하다.
설치 방법·다운로드 링크는 이 저장소에 없다.

## 2. 켜는 방법과 위험 — **미조사, 미확인**

이 저장소는 USBNetwork 설치를 시도한 적이 없다. 커뮤니티에서 통용되는
일반적인 방식(KUAL/MRPI 메뉴로 설치)은 **이 기기에서 이미 막혀 있다** — 아래
3번 참조. 그 경로가 아닌 수동 설치(스크립트 직접 배치) 방법은 조사하지
않았다. **기기 상태를 건드리는지, 초기화·OTA 위험을 높이는지는 답할 근거가
없다** — 추측하지 않는다.

🔴 **Colorsoft 를 불안정하게 만들 위험이 있는 새 시도이므로, Scribe 회수를
마치기 전까지는 착수 근거 자체가 부족하다**고 본다 (판단 재료 제공일 뿐,
지시대로 켜지 않았다).

## 3. `sqlite3` 바이너리가 기기에 있는가 — **직접 확인 안 됨, 있을 가능성 낮음**

09-19 최초 탐사(`01_tools.txt`)는 **네트워크·전송 도구만** 확인했다 — `sqlite3`
는 애초에 질의 대상이 아니었다. 그래서 **미확인**이 정확한 답이다.

다만 정황상 낮게 본다:
- 같은 탐사에서 `python`·`perl` 도 없다 — 임베디드 도구 세트가 최소한이다
- `busybox` 는 있지만(§16.6), 표준 busybox 빌드에 `sqlite3` 애플릿은 없다
  (이례적이다)
- Amazon 리더 앱 자체는 `libsqlite3.so` 를 **링크**해서 쓸 뿐, 별도
  커맨드라인 `sqlite3` 바이너리를 함께 배포할 이유가 없다

**설령 있더라도** 커뮤니티가 자체 빌드해 올린 바이너리라면 3번 아래 서술할
라이브러리 비호환 문제를 그대로 물려받을 수 있다.

## 4. macOS 에서 USBNetwork(SSH) 가 MTP·MacDroid 와 충돌하는가 — **미검증, 다른 종류의 선례만 있음**

`DEVLOG.md` §9 에 실측된 선례가 있다 — 단, **정확히 같은 상황은 아니다**:

> Voyage(구형, USB **Mass Storage** 클래스)가 마운트된 상태에서 Scribe(신형,
> **MTP** 클래스)를 MTP 로 읽으면 100% 실패(`Errno 60`)한다. Voyage 를
> 언마운트하면 즉시 정상화된다. 원인은 같은 벤더 ID(Lab126)를 다루는 macOS
> 커널 드라이버가 Mass Storage 기기를 붙잡으면서 **다른 기기**의 MTP 클레임까지
> 방해하는 것으로 추정된다.

USBNetwork 는 Mass Storage 가 아니라 **USB 이더넷 가젯(RNDIS/CDC-ECM 계열)**
인터페이스를 추가로 노출하는 방식이 일반적이다 — 이 저장소가 겪은 선례와
**USB 디바이스 클래스 자체가 다르고**, 무엇보다 선례는 "**다른 기기**끼리의
간섭"이었지 "**같은 기기**가 인터페이스를 하나 더 내밀 때 자기 자신의 MTP 를
방해하는가"는 검증된 적이 없다. macOS 가 복합(composite) USB 기기를
열거하는 방식에 따라 새 인터페이스가 기존 MTP 인터페이스의 커널 드라이버
클레임 상태를 바꿀 가능성은 있으나, **이 저장소에 그 실측은 없다.**

---

## 종합 판단

1번의 실측(사후 탈옥 상태에서도 ssh/dropbear 부재) 하나만으로도, "쉘이
붙으면 두 가지가 한꺼번에 풀린다"는 전제가 **아직 붙지 않은 쉘**을 전제로
하고 있다는 게 드러난다. 그리고 쉘을 붙이는 표준 경로(KUAL/MRPI 류)가
이 기기에서 **이미 한 번 라이브러리 비호환으로 막힌 전적**이 있어(§16.6),
같은 실패가 dropbear 에서도 재현될 개연성이 낮지 않다.

**제안**: 지금 진행 중인 staleness 방어(①②, 별도 커밋)로 "옛 사본 반입"
문제의 상당 부분은 이미 완화된다. USBNetwork 는 이득이 불확실하고 위험이
가늠 안 되는 새 시도이므로, **Scribe 회수 완료 후 Colorsoft 로 별도 실험**하는
것을 권한다(코드·기기 어느 쪽도 지금 건드리지 않았다).
