# 심볼릭 링크 제안 검토 + staleness 방어 현황

작성: 2026-09-20 · 세션 `킨들클리핑new` · 지시: 리딩총괄2 · **코드 변경 없음 (검토만)**

---

## 1. 킨들 MTP 가 심볼릭 링크를 따라가는가 — ⚠️ 미확인 (기기 미연결)

**실측할 수 없었다.** 지금 킨들이 USB 에서 빠져 있다(`system_profiler` 에 안 잡힘,
MacDroid 도 종료 상태). 기기가 연결되면 다음 절차로 바로 확인 가능하다:

```
1) MTP 로 documents/ 아래 테스트 심볼릭 링크 생성 시도
   (또는 SSH/;log 로 `ln -s /mnt/us/system/... /mnt/us/documents/test_link` 실행)
2) MTP 로 documents/ 를 다시 열거 — 링크가 객체로 보이는지, 보인다면 filetype 이 뭔지
3) 보이면 LIBMTP_Get_File_To_File 로 실제 내려받아 바이트가 대상 파일과 일치하는지 sha1 대조
```

**추론으로 대신하지 않는다** — 지시대로 확인 시도만 기록하고, 판단은 아래
2·3번 사실과 기존 실측 하나로 대체한다.

### 이미 확인된 사실 하나가 질문 자체를 흐리게 만든다

`KINDLE_ANNOTATION_OUTAGE.md` §14.12 부근에 이미 실측 기록이 있다:

> **실험 프로토콜 주의**: USB 연결 중에는 기기가 KSDK DB 연결을 닫는다
> (`USB mode is enabled, closing DB connection`).

즉 **MTP 세션이 열려 있는 동안 기기 앱은 KSDK DB 에 쓰지 않는다.** 이것이 사실이면:

- 심볼릭 링크로 "라이브 DB" 를 노출해도, 그 순간 DB 는 이미 **정지된 스냅샷**이다
  — 복사본과 실질적으로 다르지 않다
- "라이브니까 최신"이라는 링크의 장점 자체가 성립하지 않는다. USB 를 꽂아야
  읽을 수 있는데, USB 를 꽂는 순간 쓰기가 멈추기 때문이다

**결론(사실 기반, 심볼릭 링크 follow 여부와 무관)**: 링크가 기술적으로
가능하더라도 **얻는 이득이 없어 보인다.** follow 여부 확인은 여전히 유효한
질문이지만, "라이브 데이터 접근"이라는 동기 자체가 위 사실로 약해진다.

---

## 2. 라이브 DB 직접 읽기의 일관성 — WAL 사실 관계

이건 기기 없이도 답할 수 있는 SQLite 의 일반 동작이다.

- **`-wal`/`-shm` 은 부속 파일이다.** SQLite 는 메인 `.db` 를 열 때 같은
  디렉터리에 동명의 `-wal` 이 있으면 **자동으로 함께 읽어** 아직
  체크포인트되지 않은 최신 트랜잭션을 반영한다. `-shm` 은 그 인덱스용
  공유메모리 파일로, 다른 프로세스가 읽어도 의미가 있으려면 반드시
  `.db`+`-wal` 과 **같은 시점의 짝**이어야 한다
- **현재 방식(복사)도 이미 이 문제를 안고 있다.** `tools/ksdk_push.sh` 가
  `""`/`-wal`/`-shm` 세 파일을 **순차적으로** `cp` 한다 — 세 개를 원자적으로
  묶어 복사하는 게 아니다. 그 사이에 기기가 쓰면 짝이 안 맞는 사본이 생길
  수 있다. 다만 이 스크립트는 `;log mrpi` 실행 한 번 안에서 수 밀리초
  간격으로 끝나고, 실측으로 여러 번 받은 사본이 매번 sha1 이 일치했다 —
  **경험적으로는 안전했지만 이론적으로 보장된 적은 없다**
- **링크로 가면 이 창이 훨씬 커진다.** 복사는 기기 안에서 순식간에 끝나지만,
  MTP 전송은 3.6MB 파일 기준으로 수 초가 걸린다(실측: 29.9MB KFX 가 1.4초).
  그 몇 초 동안 세 파일을 순서대로 원격에서 당겨오면, 복사 방식보다 **훨씬
  넓은 시간창**에서 쓰기가 끼어들 수 있다 — 단, 1번의 "USB 연결 중엔 안 쓴다"
  사실이 맞다면 이 창도 사실상 닫혀 있다
- `-shm` 자체는 **다른 프로세스가 그대로 복사해 가도 의미가 없다** (공유
  메모리 세그먼트의 스냅샷일 뿐, 새 리더가 열 때 SQLite 가 자체적으로
  재생성/검증한다). 지금 복사 스크립트가 굳이 가져오는 이유는 **`-wal` 안의
  체크포인트 안 된 데이터를 놓치지 않기 위해서**이지 `-shm` 내용 자체가
  필요해서가 아니다. 심볼릭 링크를 셋 다 걸 필요는 없고 `.db`+`-wal` 만
  걸어도 같은 효과를 낸다(단, `-shm` 이 없으면 로컬에서 열 때 SQLite 가
  새로 만든다 — 정상 동작)

**결론**: 링크가 복사보다 나은 점을 못 찾았고, 1번 사실이 맞다면 오히려
불필요한 위험(더 넓은 전송 시간창)만 추가한다.

---

## 3. 현재 staleness 방어 — 🔴 **없다.** 이게 더 급하다

코드를 직접 확인했다. **링크냐 복사냐와 무관하게, "이 DB 가 언제 것인지"를
어디서도 기록하거나 검사하지 않는다.**

| 지점 | 파일 | 있는 것 | 없는 것 |
|---|---|---|---|
| 기기 스크립트 | `tools/ksdk_push.sh` | `push.log` 첫 줄에 `date` 헤더 있음 | 그 로그를 전송 목록(WiFi push)에 포함하지 않음 — 기기에만 남는다. 복사본 **파일명 자체는 고정**(`ksdk_annotation_v1.db`)이라 새 것/옛 것이 이름으로 구분 안 됨 |
| WiFi 수신 | `kindle/wifi.py` | 수신 시 크기·sha1 을 로그로 찍음(`on_file` 콜백) | 그 정보를 어디에도 **저장**하지 않는다 — 화면에 한 번 찍고 끝. 다음 실행에서 "저번과 같은 파일인가"를 비교할 데이터가 없다 |
| MTP 다운로드 | `kindle/device.py::_download` | — | `LIBMTP_Get_File_To_File` 로 받은 로컬 파일의 mtime 을 원본 `modificationdate` 로 맞추지 않는다(`os.utime` 호출 없음) — 로컬 mtime 은 "내려받은 시각"이지 "기기가 마지막으로 쓴 시각"이 아니다 |
| 반입(`sync_kfx.py --ksdk-db`) | `sync_kfx.py:716` 부근 | `print(f"KSDK DB: {db_path}")` + 클리핑·책 수만 출력 | **파일 mtime·sha1·DB 안의 최신 어노테이션 시각(`max(added_date)`) 어느 것도 출력하지 않는다.** 사용자가 어제 받아 둔 파일을 실수로 다시 가리켜도 아무 경고 없이 그대로 반입된다 |

**총괄이 짚은 우려가 정확하다** — "`;log mrpi` 를 안 돌린 채 파일만 당겨오면
조용히 옛 스냅샷으로 반입"되는 경로가 **지금 실제로 열려 있다.** 링크 여부와
무관하게 이미 존재하는 구멍이다.

---

## 4. 제안 (구현 안 함 — 판단용)

우선순위 순.

### ① `--ksdk-db` 반입 시 최신 어노테이션 시각을 출력한다 (가장 싸고 가장 효과적)

`parse_ksdk_db()` 가 이미 각 클리핑에 `added_date` 를 채워 준다. `sync_kfx.py`
가 DB 를 받은 직후 `max(c.added_date for c in ksdk_clips)` 를 계산해 찍기만
하면 된다 — **온디바이스 스크립트도, 전송 경로도 안 건드리고 순수 반입 쪽
한 줄**로 끝난다.

```
KSDK DB: /path/to/ksdk_annotation_v1.db
  클리핑 2977개 / 책 34권
  최신 어노테이션: 2026-09-20 21:14:03   ← 신설
```

사용자가 "어? 오늘 만든 하이라이트가 있는데 왜 어제 날짜가 최신이지"를
스스로 알아챌 수 있다. 온디바이스 변경이 없어 지금 배포된 스크립트 그대로
적용 가능하다.

### ② 파일 서명(mtime+sha1)을 상태 파일에 남기고 재사용 시 알린다

`kindle/backup.py` 가 이미 롤링 백업으로 옛 산출물을 지키고 있으니, 같은
결에서 "마지막으로 반입한 KSDK DB 의 sha1" 을 상태 파일 한 줄에 기록해 두고,
다음 실행에서 같은 sha1 이 다시 들어오면:

```
[warn] 이 KSDK DB 는 지난 실행(2026-09-20 14:32)과 sha1 이 같습니다 —
       ;log mrpi 를 다시 안 돌렸을 수 있습니다.
```

경고만 하고 막지는 않는다(같은 파일을 의도적으로 재반입할 수도 있다 — 예:
`--rewrite-bodies`).

### ③ (온디바이스 변경 필요 — 더 무겁다) push.log 도 함께 전송

`push.log` 를 WiFi push 목록과 USB 폴백 복사 목록에 추가하면, 그 안의
`date` 헤더로 "기기가 실제로 이 스크립트를 실행한 시각"을 독립적으로 확인할
수 있다. ①이 "DB 안 최신 어노테이션 시각"이라면 이건 "추출 자체를 실행한
시각"이라 상호보완적이다. 다만 기기 스크립트·수신기 화이트리스트를 둘 다
고쳐야 해서 ①·②보다 비용이 크다.

### 링크 관련 제안은 없음

1·2번 사실을 종합하면 심볼릭 링크로 얻는 이득이 안 보인다. 제안하지 않는다.
